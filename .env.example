# Public widget key, safe to expose to the browser. Create a managed Turnstile widget.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
# Private Siteverify credential. Store only in Vercel / untracked local environment.
TURNSTILE_SECRET_KEY=
# Existing email/payment/database variables remain configured separately.
